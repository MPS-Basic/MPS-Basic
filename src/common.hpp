#pragma once

#include <chrono>
#include <cstdio>
#include <filesystem>
#include <iostream>
#include <vector>

using std::cerr;
using std::cout;
using std::endl;
using std::vector;
namespace fs     = std::filesystem;
namespace chrono = std::chrono;