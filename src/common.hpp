#pragma once

#include <chrono>
#include <cstdio>
#include <filesystem>
#include <iostream>
#include <vector>


#define rep(i, a, b) for (int i = a; i < b; i++)

using std::cerr;
using std::cout;
using std::endl;
using std::vector;
namespace fs     = std::filesystem;
namespace chrono = std::chrono;